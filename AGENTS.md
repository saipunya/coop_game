# Current Focus

- Treat `yang/` (served at `/yang`) as the active development area.
- Keep changes inside `yang/` unless the task requires an integration change elsewhere.
- Relevant integration files such as `app.js`, `.env.example`, and database setup files may be changed when needed for `/yang` to work.
- Do not modify the existing game application outside this scope unless explicitly requested.
- Preserve unrelated working-tree changes.
